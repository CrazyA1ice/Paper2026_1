name: Build v21 manuscript
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/build_v21.yml'
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk fonts-liberation libreoffice-writer poppler-utils
          python -m pip install --upgrade pip
          python -m pip install python-docx pillow
      - name: Build v21
        run: python word_output/rewrite_v20_to_v21_from_aigc_report.py
      - name: Audit and render
        run: |
          python - <<'PY'
          from docx import Document
          p="word_output/2026.09.21v21.docx"
          d=Document(p)
          text="\n".join(x.text for x in d.paragraphs)
          assert len(d.tables)==3
          assert len(d.inline_shapes)==4
          for x in ["4.06%","4.43%","29.89%","46.83%","1.67%","4.68%","0.186","0.434","2.77%","2.37%","0.257","0.319","0.424","0.283","0.318","0.399","4656","8208","8339","131"]:
              assert x in text
          print("paragraphs",len(d.paragraphs),"tables",len(d.tables),"figures",len(d.inline_shapes))
          PY
          rm -rf word_output/render_v21_word
          mkdir -p word_output/render_v21_word
          libreoffice --headless --convert-to pdf --outdir word_output/render_v21_word word_output/2026.09.21v21.docx
          pdftoppm -png -r 170 word_output/render_v21_word/2026.09.21v21.pdf word_output/render_v21_word/page
          pages=$(find word_output/render_v21_word -name 'page-*.png' | wc -l)
          echo "Rendered pages: $pages"
          test "$pages" -ge 8
          test "$pages" -le 11
      - name: Commit v21
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add word_output/2026.09.21v21.docx word_output/render_v21_word/
          git commit -m "Revise v20 using AIGC report hotspots" || exit 0
          git push
