# 论文完整性与一致性检查报告

## 1. 检查范围

- 稿件：`MANUSCRIPT_FRONT_HALF_CN.md`，现已扩展为正文完整初稿。
- 对照材料：模型代码、数据处理代码、正式消融报告及 `results/recommended_analysis/` 下的汇总表。
- 检查内容：研究主线、公式—代码一致性、数值一致性、统计表述、术语和引文、过度结论、篇幅与段落功能。

## 2. 已完成修正

1. 修正自适应路由统计向量公式中缺失的 `\\left` 反斜杠。
2. 修正完整模型参数量表述：参数量随频率保留数变化，\(r=0.50\) 为 8381，\(r=0.75\) 为 8402；不再统一写成 8381。
3. 将论文结构统一为“0 引言—1 相关工作—2 方法—3 实验与结果分析—4 结论”。
4. 在统计方法中明确：每配置 \(n=3\) 指三个随机种子，813 个测试窗口不是独立重复。
5. 对 \(r=0.50\) 的两项预设权重比较明确使用 Holm 校正；对 \(r=0.75\) 的追加比较标为探索性结果。
6. 将频域机制的解释从确定性因果表述降为“与结果一致的可能解释”，并保留完整模型未超过无频域主模型的结论。

## 3. 数值核对

| 核对项 | 稿件数值 | 数据文件 | 结论 |
|---|---:|---|---|
| DLinear MSE | 0.193983 | `ablation_mean_std.csv` | 一致 |
| 自适应多尺度 MSE | 0.186099 | `ablation_mean_std.csv` | 一致 |
| 相对 DLinear MSE 改善 | 4.06% | 由两均值计算 | 一致 |
| 无频域自适应相对固定等权 | 3.95% | `paired_tests.csv` | 一致 |
| \(r=0.75\) 自适应相对固定等权 | 3.74% | `final_cut075_paired_test.csv` | 一致 |
| \(r=0.75\) 配对检验 | \(t(2)=3.469,p=0.07401\) | 同上 | 一致 |
| 完整模型相对 DLinear | MSE 2.34%、MAE 9.28% | 由汇总均值计算 | 一致 |

## 4. 结果分配与主文压缩

| 结果 | 类型 | 主文处理 |
|---|---|---|
| 自适应多尺度相对 DLinear | 核心发现 | 表1和3.3正文 |
| 固定/自适应权重对照 | 必要归因证据 | 表2和3.4正文 |
| 三档频率比例 | 结论限定与敏感性 | 表3和3.5正文 |
| 路由权重随样本变化 | 必要支持 | 仅保留一句，不解释尺度语义 |
| 逐种子运行值、训练日志、全部预测数组 | 溯源细节 | 保留在结果目录，不重复进入正文 |
| 原始流量单位 RMSE | 非核心且数值尺度异常敏感 | 不进入主表，正文明确采用标准化空间指标 |

## 5. 统计审查

- P0：未发现会直接推翻当前核心结论的统计书写错误。
- P1：每配置只有三个随机种子，配对 \(t\) 检验的分布假设无法可靠检查；稿件已将描述性均值、标准差和效应方向作为主要证据。
- P1：\(r=0.75\) 是比较三个测试结果后得到的候选较优值，不属于独立验证的调参结论；稿件已统一改为“探索性较优设置”。
- P1：仅使用单一数据集且缺少相同协议下的 RSTIF、FlowDiviner、CycleLLH 等强网络流量模型实测对比，可能成为审稿人对实验完整性的主要质疑。当前稿件通过收窄为应用型轻量消融研究降低该风险，但不能消除。
- P2：正文暂未包含预测曲线、误差分布和路由权重图。它们有助于展示应用效果，但不是当前数值结论成立的前提。

## 6. 术语与主张检查

- 统一使用“自适应多尺度加权”“DLinear 专家”“频域门控”“频率保留比例 \(r\)”和“OD 流量矩阵”。
- 未使用“首次提出”“达到 SOTA”“显著优于所有方法”或“频域模块必然提升精度”等无证据措辞。
- 引言提出的三项工作均在方法或实验章节得到对应说明。
- 结论没有引入新数据，且保留单数据集、三随机种子、无拓扑建模三项边界。

## 7. 投稿前仍建议完成

1. 生成摘要、关键词和英文题名/摘要；当前用户任务未要求，故未补写。
2. 从已保存的 `predictions.npz` 生成一张典型 OD 真实值—预测值曲线，并从汇总 CSV 生成一张频率敏感性图。
3. 按目标期刊模板统一表格、公式编号和参考文献格式。
4. 若时间允许，补一个第二数据集或至少一个不同预测长度；这属于增强泛化证据，不是当前 Abilene 内部消融结论的必要条件。
